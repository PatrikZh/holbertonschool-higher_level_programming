#!/usr/bin/python3
def number_keys(a_dictionary):
    count = 0
    for key, value in a_dictionary.items():
            count += 1
    return count

# count_keys = len(a_dictionary)
# return count_keys -> it will return the number of keys
